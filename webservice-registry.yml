server:
 port: 8761
 

eureka:
 instance:
   hostname: localhost
 client:
   registerWithEureka: false
   fetchRegistry: false
   serviceUrl: 8080
    defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
